@model IEnumerable<CategorySummary>

@{
    Layout = null;
}

<h3>Categories</h3>
<ul class="list-group--alt">
    @foreach (var category in Model)
    {
        <li><a href="#" title="@category.ShortDescription">@category.Title</a></li>
    }
</ul>